{
	"notOwned": "this daemon does not own \"{name}\"",
	"invalidMode": "invalid daemon mode: {mode} (expected primary or follower)",
	"followerNeedsPrimary": "follower mode requires primary.address (or LUNA_PRIMARY_ADDRESS)",
	"followerNotConnected": "follower \"{name}\" is not connected",
	"rootMissing": "cluster root does not exist: {root}",
	"seededRegistry": "seeded an empty cluster registry at {path}",
	"alreadyRunning": "a daemon is already running on {socket}",
	"noClusterLink": "no cluster link",
	"removeFailed": "daemon {daemon} could not remove {name}: {detail}",
	"noFollowerLink": "instance targets daemon \"{name}\" but no follower link exists",
	"primaryOnlyUpgrade": "only the primary daemon can upgrade another daemon",
	"primaryOnlyCheck": "only the primary daemon can check another daemon for upgrades",
	"unknownOp": "unknown operation: {op}",
	"consoleTimeout": "daemon \"{name}\" did not answer the console request; its build may predate console tunnelling",
	"consoleUnreachable": "daemon \"{name}\" is not connected; its console is unreachable",
	"backupUnreachable": "backup archive on daemon \"{daemon}\" could not be reached",
	"mapUnreachable": "daemon \"{daemon}\" is not connected; its rendered map is unreachable",
	"machineUnreachable": "{machine} is unreachable; its plugin port configuration was not checked",
	"checks": {
		"link": "Daemon link",
		"heartbeat": "Heartbeat",
		"reachability": "Instance reachability",
		"headroom": "Resource headroom",
		"listener": "Cluster listener",
		"token": "Cluster token",
		"build": "Build",
		"noLink": "no link",
		"lastSeen": "not connected; last seen {time}",
		"neverConnected": "never connected",
		"beatOk": "{latency}ms round-trip · last beat {ago} ago",
		"beatMissed": "{count} missed ping(s); last beat {ago} ago",
		"selfLink": "this daemon; local socket {socket}",
		"listenerOn": "accepting followers on {host}:{port}; {count} connected",
		"noListener": "no TCP listener configured; this host cannot accept followers",
		"tokenConfigured": "configured; followers must present it",
		"tokenMissing": "not configured; followers cannot authenticate",
		"buildMatch": "running {version} · the same build as this primary",
		"buildMismatch": "running {version} · this primary runs {current}; upgrade this daemon",
		"buildReference": "running {version} · the build every follower is compared against",
		"noSample": "no health sample yet",
		"cpuAt": "CPU at {pct}%",
		"memAt": "memory at {pct}%",
		"diskAt": "disk at {pct}%",
		"swapAt": "swap {pct}%",
		"headroomOk": "CPU {cpu}% · memory {mem}% · disk {disk}% · load {load}",
		"notProbed": "not probed yet",
		"nothingToProbe": "no running instance to probe on {host}",
		"unreachable": "unreachable: {names}",
		"answering": "{count} port(s) answering on {host}",
		"quarantinedBeat": "answering, but the link is refused · last beat {ago} ago"
	},
	"upgrade": {
		"noPrimarySource": "primary: this daemon has no primary to upgrade from",
		"platformMismatch": "primary: its binary is {theirs}, this machine is {ours}",
		"noAsset": "github: no release with a {asset} asset in {repo}",
		"refused": "{origin} refused the binary: HTTP {status}",
		"sizeMismatch": "size mismatch; got {got} bytes, expected {expected}",
		"checksumMismatch": "checksum mismatch; got {got}, expected {expected}",
		"jobsRunning": "{count} job(s) still running; wait for them to settle, or force the upgrade",
		"sourceNoBinary": "this daemon runs from source, so it has no binary to serve; build one with `bun run build`",
		"sourceNoUpgrade": "this daemon runs from source; restart it from the new tree instead of upgrading",
		"dirNotWritable": "cannot replace {path}: {dir} is not writable by this daemon; re-run \"sudo luna setup\" here to move it onto a binary the service account owns",
		"nothingNewer": "already running {version}; nothing newer on any channel{why}",
		"noSourceWhy": "no upgrade source could be reached{why}",
		"received": "{got} of {total}",
		"steps": {
			"preflight": "Preflight",
			"resolving": "Upgrade source",
			"download": "Binary",
			"install": "Install",
			"checking": "checking the binary, the running jobs and the install directory",
			"checked": "clear to upgrade",
			"asking": "asking every channel what it has",
			"resolved": "{version} from {origin}",
			"fetching": "fetching {size} from {origin}",
			"verified": "{size} downloaded and verified",
			"swapping": "swapping the binary",
			"restarting": "{from} → {to}; restarting",
			"console": "web console"
		},
		"autoApplying": "auto-upgrade: installing {version} from the {origin}",
		"autoFailed": "auto-upgrade deferred: {error}"
	},
	"rootNotFound": "cluster root not found; set root in the daemon config, LUNA_ROOT, or run inside the cluster directory",
	"log": {
		"protocolMismatch": "protocol mismatch; attempting a self-upgrade from the primary",
		"linkLost": "primary link lost ({code}); retrying in {seconds}s",
		"starting": "luna daemon \"{name}\" ({mode}), root {root}",
		"noToken": "warning: no cluster token configured; followers cannot authenticate",
		"shuttingDown": "{signal} received, shutting down",
		"droppingLink": "follower \"{name}\" missed {count} heartbeats; dropping the link",
		"autoUpgradeOn": "auto-upgrade on; a newer build is installed as it is found",
		"autoUpgradeOff": "auto-upgrade off for this daemon (policy: {policy})",
		"quarantined": "the primary refused this build: {reason}",
		"protocolMismatchNoAuto": "protocol mismatch, but auto-upgrade is off; this daemon stays quarantined until upgraded"
	},
	"sampler": {
		"servingTelemetry": "serving telemetry; {count} backend(s) reporting",
		"noHeartbeatSince": "no heartbeat since {ago}; the plugin has stopped reporting",
		"neverReported": "never reported to the proxy; check the LunaCore config",
		"responding": "responding; {online}/{max} players",
		"tickCheck": "Tick performance",
		"tickNotReported": "not reported by this backend",
		"tickApdex": "apdex {apdex}",
		"tickMean": "mean {mean}ms",
		"tickMisery": "misery {misery}%"
	},
	"clientUnavailable": "luna daemon is not running on this host; start it with `luna daemon run`, or install the service: `luna daemon service install`",
	"protocolSkew": "daemon protocol {daemon} does not match this binary ({client}); restart the daemon from the same build",
	"invalidAutoUpgrade": "invalid autoUpgrade policy: {value} (expected {allowed})",
	"quarantinedOp": "daemon \"{name}\" is quarantined and cannot be given work: {reason}",
	"quarantine": {
		"protocol": "protocol mismatch: hub {hub}, follower {follower}",
		"ahead": "follower \"{name}\" speaks a newer protocol than this primary; upgrade the primary instead",
		"pushing": "pushing a recovery upgrade to quarantined follower \"{name}\"",
		"pushFailed": "recovery upgrade for \"{name}\" did not complete: {error}"
	},
	"console": {
		"extractFailed": "could not unpack the console bundle: {error}",
		"noBuild": "{asset} contained no build/index.js; not a console bundle",
		"installed": "console {version} installed",
		"skipFollower": "no console on a follower; skipped",
		"noAsset": "{origin} publishes no console bundle; the console is unchanged",
		"fetching": "fetching the console bundle ({size})",
		"failed": "console not updated: {error}"
	},
	"stageTooLarge": "The upload exceeds the maximum staged world size.",
	"stageMissing": "No staged world {token} on this machine.",
	"stageFetchFailed": "Could not fetch staged world {token} from the primary: HTTP {status}",
	"noPrimaryForStage": "This daemon has no primary to fetch a staged world from."
}
