{
  "name": "luna-control",
  "version": "1.0.0",
  "module": "src/cli/index.ts",
  "type": "module",
  "private": true,
  "license": "SEE LICENSE IN LICENSE",
  "scripts": {
    "dev": "bun run src/cli/index.ts",
    "build": "bun run scripts/genversion.ts && bun build --compile --minify src/cli/index.ts --outfile dist/luna",
    "typecheck": "bunx tsc --noEmit"
  },
  "devDependencies": {
    "@types/bun": "latest"
  },
  "peerDependencies": {
    "typescript": "^5"
  },
  "dependencies": {
    "@clack/prompts": "^1.7.0",
    "picocolors": "^1.1.1",
    "yaml": "^2.9.0"
  }
}
